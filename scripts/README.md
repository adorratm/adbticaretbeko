# Helper scripts
# - scripts/dev-backends.ps1 — tüm Go backend'leri başlat
# - scripts/smoke-checkout.ps1 — sepet → checkout → ödeme → fatura smoke (`yarn smoke:checkout`)
# - scripts/sync-env.ps1 — `.env.example` anahtarlarını `.env`'e ekler (`yarn sync:env`)
