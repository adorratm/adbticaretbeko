# Shipment Service

Kargo / sevkiyat soyutlaması (mock provider).

- `POST /v1/shipments`
- `GET /v1/shipments/by-order/{orderId}`
- `PATCH /v1/shipments/{id}/status`

Port: `8091` · Schema: `shipment`
